{{ form_start(form) }}
    {{ form_widget(form) }}
    <hr>
    <div class="d-flex gap-2 justify-content-center">
        <button class="btn btn-success"><i class="bi bi-floppy"></i> {{ button_label|default('Save') }}</button>

        <a href="{{ path('app_fk_category_index') }}" class="btn btn-secondary"><i class="bi bi-box-arrow-left"></i> Back to list</a>
    </div>
{{ form_end(form) }}
