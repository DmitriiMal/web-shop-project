{% extends 'base.html.twig' %}

{% block title %}FkCategory{% endblock %}

{% block body %}
    <h1>FkCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fk_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ fk_category.name }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ fk_category.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fk_category_index') }}">back to list</a>

    <a href="{{ path('app_fk_category_edit', {'id': fk_category.id}) }}">edit</a>

    {{ include('fk_category/_delete_form.html.twig') }}
{% endblock %}
