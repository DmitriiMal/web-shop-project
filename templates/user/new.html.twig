{% extends 'base.html.twig' %}

{% block title %}Add new User | {{ parent() }}{% endblock %}

{% block body %}
/* <style>
    .s_password{
        display:none;
    }
</style> */


<div class="container-prim">
    <h1>Add new User</h1>

    {{ include('user/_create_form.html.twig', {'button_label': 'Update'}) }}

    {# <a href="{{ path('app_user_index') }}">back to list</a> #}
</div>
{% endblock %}
